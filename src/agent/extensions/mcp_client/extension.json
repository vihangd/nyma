{
  "namespace": "mcp-client",
  "description": "MCP server integration — third-party tools from `.mcp.json` / `settings.mcp`",
  "capabilities": [
    "events",
    "tools",
    "tools-override",
    "commands",
    "ui"
  ],
  "dependsOn": [
    "agent-shell"
  ]
}
