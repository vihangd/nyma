{
  "namespace": "agent-state",
  "description": "Reports what nyma is doing (idle, streaming, tool, waiting) to a supervisor process via a command hook",
  "capabilities": [
    "events",
    "spawn"
  ],
  "dependsOn": [],
  "settings": {
    "agent-state": {
      "enabled": null,
      "agent": "nyma",
      "command": null
    }
  }
}
